#include <iostream>
#include <cmath>
using namespace std;

int max(int a, int b)
{
    if (a > b)
        return a;
    else
        return b;
}
int main()
{
    // cout << max(51, 78);
    // cout << min(51, 78,1); //build in only works for 2 no
    // cout<<pow(2.6,2.6);
    // cout<<sqrt(64)<<endl;
    // cout<<cbrt(64)<<endl;
    cout << abs(-5) << endl; // Mod math vala
}